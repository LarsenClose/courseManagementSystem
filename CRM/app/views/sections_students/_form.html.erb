<%= form_with(model: sections_student, local: true) do |form| %>
  <% if sections_student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sections_student.errors.count, "error") %> prohibited this sections_student from being saved:</h2>

      <ul>
        <% sections_student.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :section_id %>
    <%= form.text_field :section_id %>
  </div>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
